import { type NextPage } from "next";
import { Button } from "@chakra-ui/react";
const Home: NextPage = () => {
  return (
    <>
      <Button colorScheme="blue" size="md">
        Click me!
      </Button>
    </>
  );
};

export default Home;
